<Window x:Class="GaokaoCountdown.ClassOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title=""
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        WindowStartupLocation="CenterScreen"
        Width="500" Height="280"
        SizeToContent="WidthAndHeight">

    <Border x:Name="RootBorder"
            Background="#CC1A1A2E"
            BorderBrush="#44FFFFFF"
            BorderThickness="1"
            CornerRadius="16"
            Padding="30"
            SnapsToDevicePixels="True"
            RenderOptions.ClearTypeHint="Enabled"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform x:Name="RootScale" ScaleX="0.9" ScaleY="0.9"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect ShadowDepth="4" BlurRadius="30" Opacity="0.6" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 主文字：大字居中 -->
            <TextBlock x:Name="MainText"
                       Grid.Row="0"
                       FontSize="52"
                       FontWeight="Bold"
                       Foreground="#FFFFFFFF"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       RenderOptions.ClearTypeHint="Enabled">
                <TextBlock.Effect>
                    <DropShadowEffect ShadowDepth="1" BlurRadius="8" Opacity="0.3" Color="#000000"/>
                </TextBlock.Effect>
            </TextBlock>

            <!-- 副文字 -->
            <TextBlock x:Name="SubText"
                       Grid.Row="1"
                       FontSize="18"
                       Foreground="#99CCCCDD"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       Margin="0,12,0,0"
                       TextWrapping="Wrap">
            </TextBlock>

            <!-- 关闭按钮 -->
            <Button x:Name="CloseBtn"
                    Grid.Row="2"
                    Content="✕  关闭"
                    Width="100" Height="36"
                    Margin="0,20,0,0"
                    FontSize="13"
                    HorizontalAlignment="Center"
                    Background="#22FFFFFF"
                    Foreground="#AACCCCEE"
                    BorderBrush="#30FFFFFF"
                    BorderThickness="1"
                    Cursor="Hand"
                    Click="CloseBtn_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform/>
                            </Border.RenderTransform>
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#33FFFFFF"/>
                                <Setter Property="Foreground" Value="#FFFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetName="border"
                                                Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                To="0.92" Duration="0:0:0.08">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation
                                                Storyboard.TargetName="border"
                                                Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                To="0.92" Duration="0:0:0.08">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>
